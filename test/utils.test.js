import {
    canAllDrink,
    canSomeoneDrink,
    cesarCodex,
    characterFilter,
    findInObj,
    isIsogram,
    matrixLetter,
    maxItems,
    moreCommonWord,
    oddAndEven,
    palindro,
    scoreAverage,
    countSheep,
} from '../src/utils';

test('canAllDrink', () => {
    expect(true).toBe(true);
});
